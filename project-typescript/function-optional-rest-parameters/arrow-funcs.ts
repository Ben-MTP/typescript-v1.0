

function sum(a: number, b: number): number {
  return a + b;
}

console.log(`Method sum: `+sum(100, 200));

let add = (a: number, b: number): number => {
  return a + b;
}

console.log(`Method Add: `+add(100, 200));